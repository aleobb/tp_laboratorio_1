#include <stdio.h>
#include <stdlib.h>

#include "funcAux.h"
#include <string.h>
#include "Productos.h"

/*
Usuario* newUsuario(int id, char nombre[], char password[], int contadorCalificaciones, int acumuladorCalificaciones, int isEmpty)
{
    Usuario* returnAux = NULL;
    Usuario* pUsuario = malloc(sizeof(Usuario));

    if(pUsuario != NULL)
    {
        pUsuario->id = id;
        strcpy(pUsuario->nombre,nombre);
        strcpy(pUsuario->password,password);
        pUsuario->contadorCalificaciones = contadorCalificaciones;
        pUsuario->acumuladorCalificaciones = acumuladorCalificaciones;
        pUsuario->isEmpty = ;
        returnAux = pUsuario;
    }
    return returnAux;
}
*/
